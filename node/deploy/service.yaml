apiVersion: v1
kind: Service
metadata:
  name: grok2api
  labels:
    app.kubernetes.io/name: grok2api
    app.kubernetes.io/component: node
spec:
  selector:
    app.kubernetes.io/name: grok2api
    app.kubernetes.io/component: node
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
